name: Turni Telegram Bot

on:
  workflow_dispatch:
  schedule:
    - cron: "0 7 * * 1"   # Lunedì mattina → sender
    - cron: "0 18 * * 4"  # Giovedì sera → reminder
    - cron: "0 7 * * 6"   # Sabato mattina → promemoria domenica

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      # =====================
      # CHECKOUT
      # =====================
      - name: Checkout repo
        uses: actions/checkout@v4

      # =====================
      # PYTHON
      # =====================
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      # =====================
      # DEPENDENCIES
      # =====================
      - name: Install dependencies
        run: pip install flask requests supabase pandas openpyxl

      # =====================
      # LUNEDÌ → SENDER
      # =====================
      - name: Run sender
        if: github.event.schedule == '0 7 * * 1'
        run: python sender.py
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}

      # =====================
      # GIOVEDÌ → REMINDER
      # =====================
      - name: Run reminder
        if: github.event.schedule == '0 18 * * 4'
        run: python reminder.py
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}

      # =====================
      # SABATO → PROMEMORIA DOMENICA
      # =====================
      - name: Run sunday reminder
        if: github.event.schedule == '0 7 * * 6'
        run: python reminder_sunday.py
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}

      # =====================
      # COMMIT STATO BOT
      # =====================
      - name: Commit bot state
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git add .
          git diff --staged --quiet || git commit -m "update bot state"

      # =====================
      # PUSH FIXATO (NO 403)
      # =====================
      - name: Push changes
        uses: ad-m/github-push-action@v0.8.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
